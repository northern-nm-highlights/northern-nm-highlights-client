<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="edu.cnm.deepdive.nothernnmhighlightsclient">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.Nothernnmhighlightsclient">
    <activity android:name=".controller.FavoritePlaceFragment">
    </activity>
    <activity android:name=".controller.PlaceTypeFragment">
    </activity>
    <activity android:name=".LoginActivity">
    </activity>
    <activity
      android:exported="true"
      android:name=".controller.LoginActivity">
      <intent-filter>

        <!-- Launches app -->
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>

  <uses-permission android:name="android.permission.INTERNET"/>

</manifest>