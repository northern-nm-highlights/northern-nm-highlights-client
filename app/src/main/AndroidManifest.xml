<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="edu.cnm.deepdive.nothernnmhighlightsclient">

  <uses-permission android:name="android.permission.INTERNET"/>

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.Nothernnmhighlightsclient">
    <activity android:name="edu.cnm.deepdive.nothernnmhighlightsclient.LoginActivity">
    </activity>
    <activity android:name="edu.cnm.deepdive.nothernnmhighlightsclient.MainActivity1">
    </activity>
  <activity android:name=".controller.LoginActivity"
    android:exported="true">

    <intent-filter>

      <!-- Launches app -->
      <action android:name="android.intent.action.MAIN"/>

      <category android:name="android.intent.category.LAUNCHER"/>
    </intent-filter>
    </activity>
  </application>

</manifest>